// input: 'Tact cat'
// output: true (permutations: 'taco cat', 'atco cta')

function isPalindrom(str) {
    let arr = str.split()
    for (let el in str) {
        
    } 
}

let str = "taco cat"

console.log(str.split(/\s+/));
